# Generated by openapi-operator-gen {{ .GeneratorVersion }}
# Example {{ .Kind }} - Adopt and modify an existing resource
#
# This CR demonstrates adopting an existing resource from the REST API
# and changing one of its fields. When deleted, the resource will be
# restored to its original state (onDelete: Restore).
#
# Use case: Temporarily modify an existing resource under Kubernetes control
apiVersion: {{ .APIGroup }}/{{ .APIVersion }}
kind: {{ .Kind }}
metadata:
  name: {{ .KindLower }}-adopt-and-modify
  namespace: default
spec:
{{- range .SpecFields }}
{{- if .IncludeInAdopt }}
  {{ .JSONName }}: {{ .AdoptValue }}
{{- end }}
{{- end }}

  # Restore original state when this CR is deleted
  onDelete: "Restore"

  # Endpoint targeting (optional - uncomment if needed)
  # targetNamespace: "target-namespace"
  # targetStatefulSet: "my-statefulset"
  # targetDeployment: "my-deployment"
  # targetHelmRelease: "my-release"
  # targetPodOrdinal: 0
