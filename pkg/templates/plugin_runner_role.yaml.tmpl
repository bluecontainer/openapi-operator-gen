# Generated by openapi-operator-gen {{ .GeneratorVersion }}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ .AppName }}-plugin-runner-role
  labels:
    app.kubernetes.io/name: {{ .AppName }}
    app.kubernetes.io/component: kubectl-plugin
    app.kubernetes.io/managed-by: openapi-operator-gen
rules:
# Pod management (for kubectl run ephemeral pods in k8s-based Rundeck jobs)
- apiGroups: [""]
  resources: ["pods"]
  verbs: ["create", "delete", "list", "get"]
# Pod attach and logs (for kubectl run -i streaming output)
- apiGroups: [""]
  resources: ["pods/attach", "pods/log"]
  verbs: ["get", "create"]
# Workload discovery (for nodes command / Rundeck resource model source)
- apiGroups: ["apps"]
  resources: ["statefulsets", "deployments"]
  verbs: ["list", "get"]
- apiGroups: [""]
  resources: ["services"]
  verbs: ["list"]
