# Generated by openapi-operator-gen {{ .GeneratorVersion }}
# Cleanup one-shot resources (Docker execution)
- defaultTab: output
  description: |-
    Clean up completed one-shot resources (queries and actions).
    Removes resources that have finished executing.
    Runs kubectl plugin inside a Docker container.
    Uses: docker run ... kubectl {{ .PluginName }} cleanup
  executionEnabled: true
  group: operations
  loglevel: INFO
  name: cleanup
  nodeFilterEditable: false
  options:
  - name: namespace
    description: "Kubernetes namespace (default: from kubeconfig)"
  - name: dry_run
    description: "Preview what would be cleaned up without deleting"
    enforced: true
    values:
    - 'true'
    - 'false'
    value: 'false'
  scheduleEnabled: true
  sequence:
    commands:
    - description: "Cleanup one-shot resources"
      scriptInterpreter: /bin/bash
      script: |-
        set -e
        IMAGE="${PLUGIN_RUNNER_IMAGE:-{{ .PluginName }}-kubectl-plugin:latest}"
        NET="${DOCKER_NETWORK:-host}"
        KUBE_VOL="${DOCKER_KUBE_VOLUME:-}"
        KUBE="${KUBECONFIG:-/root/.kube/config}"
        if [ -n "$KUBE_VOL" ]; then
          VOL_ARGS="-v $KUBE_VOL:/kube:ro -e KUBECONFIG=/kube/kube/config"
        else
          VOL_ARGS="-v $KUBE:/root/.kube/config:ro"
        fi
        cmd="docker run --rm --network $NET $VOL_ARGS $IMAGE {{ .PluginName }} cleanup"
        [ -n "$RD_OPTION_NAMESPACE" ] && cmd="$cmd -n $RD_OPTION_NAMESPACE"
        cmd="$cmd --dry-run=$RD_OPTION_DRY_RUN"
        eval $cmd
    keepgoing: false
    strategy: node-first
