# Generated by openapi-operator-gen v0.0.10-10-ge5a6a04-dirty
# Cleanup one-shot resources (Docker execution)
- defaultTab: output
  description: |-
    Clean up completed one-shot resources (queries and actions).
    Removes resources that have finished executing.
    Runs kubectl plugin inside a Docker container.
    Uses: docker run ... kubectl petstore cleanup
  executionEnabled: true
  group: operations
  loglevel: INFO
  name: cleanup
  nodeFilterEditable: false
  options:
  - name: namespace
    description: "Kubernetes namespace"
  - name: dry_run
    description: "Preview what would be cleaned up without deleting"
    enforced: true
    values:
    - 'true'
    - 'false'
    value: 'false'
  - name: k8s_token
    secure: true
    valueExposed: true
    storagePath: keys/project/petstore-operator-docker/k8s-token
    hidden: true
  scheduleEnabled: true
  sequence:
    commands:
    - description: "Cleanup one-shot resources"
      scriptInterpreter: /bin/bash
      script: |-
        set -e
        IMAGE="${PLUGIN_RUNNER_IMAGE:-petstore-kubectl-plugin:latest}"
        NET="${DOCKER_NETWORK:-host}"
        cmd="docker run --rm --network $NET $IMAGE petstore cleanup"
        [ -n "$RD_OPTION_NAMESPACE" ] && cmd="$cmd -n $RD_OPTION_NAMESPACE"
        cmd="$cmd --dry-run=$RD_OPTION_DRY_RUN"
        cmd="$cmd --server=\"$RD_GLOBALS_K8S_URL\" --token=\"$RD_OPTION_K8S_TOKEN\" --insecure-skip-tls-verify"
        eval $cmd
    keepgoing: false
    strategy: node-first
