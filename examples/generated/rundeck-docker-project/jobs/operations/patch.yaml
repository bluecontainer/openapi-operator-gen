# Generated by openapi-operator-gen v0.0.10-20-ge0145f7-dirty
# Patch a resource (Docker execution)
- defaultTab: output
  description: |-
    Patch or restore a Kubernetes resource spec.
    Can apply JSON patches, set TTL, or restore from REST API.
    Runs kubectl plugin inside a Docker container.
    Uses: docker run ... kubectl petstore patch {kind} {name}
  executionEnabled: true
  group: operations
  loglevel: INFO
  name: patch
  nodeFilterEditable: true
  nodefilters:
    dispatch:
      threadcount: '1'
      keepgoing: 'false'
      rankOrder: ascending
    filter: '.*'
  options:
  - name: resource_kind
    description: "Resource kind (e.g., Pet, Order, User)"
    required: true
  - name: resource_name
    description: "Resource name"
    required: true
  - name: spec
    description: "JSON spec patch to apply"
  - name: ttl
    description: "TTL for the resource (e.g., 30m, 1h)"
  - name: restore
    description: "Restore original spec from REST API"
    enforced: true
    values:
    - 'true'
    - 'false'
    value: 'false'
  - name: dry_run
    description: "Preview the patch without applying"
    enforced: true
    values:
    - 'true'
    - 'false'
    value: 'false'
  - name: output
    description: "Output format"
    enforced: true
    values:
    - 'table'
    - 'json'
    - 'yaml'
    value: 'json'
  - name: namespace
    description: "Kubernetes namespace (defaults to project setting)"
    value: '${globals.namespace}'
  - name: target_pod_ordinal
    description: "Target a specific pod ordinal index"
  - name: target_base_url
    description: "Target a specific base URL"
  - name: target_base_urls
    description: "Target multiple base URLs (comma-separated)"
  - name: target_statefulset
    description: "Target pods in a specific StatefulSet"
  - name: target_deployment
    description: "Target pods in a specific Deployment"
  - name: target_pod
    description: "Target a specific pod by name"
  - name: target_helm_release
    description: "Target pods from a specific Helm release"
  - name: target_namespace
    description: "Target pods in a specific namespace"
  - name: target_labels
    description: "Target pods matching label selector"
  - name: k8s_token
    secure: true
    valueExposed: true
    storagePath: keys/${node.clusterTokenSuffix}
    hidden: true
  - name: k8s_token_default
    secure: true
    valueExposed: true
    storagePath: keys/project/petstore-operator-docker/k8s-token
    hidden: true
  scheduleEnabled: true
  sequence:
    commands:
    - description: "Patch resource"
      scriptInterpreter: /bin/bash
      nodeStep: true
      script: |-
        set -e
        IMAGE="${PLUGIN_RUNNER_IMAGE:-petstore-kubectl-plugin:latest}"
        NET="${DOCKER_NETWORK:-host}"
        cmd="docker run --rm --network $NET $IMAGE petstore patch $RD_OPTION_RESOURCE_KIND $RD_OPTION_RESOURCE_NAME"
        [ -n "$RD_OPTION_SPEC" ] && cmd="$cmd --spec=\"$RD_OPTION_SPEC\""
        [ -n "$RD_OPTION_TTL" ] && cmd="$cmd --ttl=\"$RD_OPTION_TTL\""
        [ "$RD_OPTION_RESTORE" = "true" ] && cmd="$cmd --restore"
        [ "$RD_OPTION_DRY_RUN" = "true" ] && cmd="$cmd --dry-run"
        [ -n "$RD_OPTION_OUTPUT" ] && cmd="$cmd --output $RD_OPTION_OUTPUT"
        _NS="${RD_OPTION_NAMESPACE:-$RD_GLOBALS_NAMESPACE}"
        [ -n "$_NS" ] && cmd="$cmd -n $_NS"
        # Hybrid targeting: explicit options override node attributes
        if [ -n "$RD_OPTION_TARGET_STATEFULSET" ] || [ -n "$RD_OPTION_TARGET_DEPLOYMENT" ] || \
           [ -n "$RD_OPTION_TARGET_HELM_RELEASE" ] || [ -n "$RD_OPTION_TARGET_BASE_URL" ] || \
           [ -n "$RD_OPTION_TARGET_POD" ]; then
          [ -n "$RD_OPTION_TARGET_POD_ORDINAL" ] && cmd="$cmd --target-pod-ordinal=$RD_OPTION_TARGET_POD_ORDINAL"
          [ -n "$RD_OPTION_TARGET_BASE_URL" ] && cmd="$cmd --target-base-url=\"$RD_OPTION_TARGET_BASE_URL\""
          [ -n "$RD_OPTION_TARGET_BASE_URLS" ] && cmd="$cmd --target-base-urls=\"$RD_OPTION_TARGET_BASE_URLS\""
          [ -n "$RD_OPTION_TARGET_STATEFULSET" ] && cmd="$cmd --target-statefulset=\"$RD_OPTION_TARGET_STATEFULSET\""
          [ -n "$RD_OPTION_TARGET_DEPLOYMENT" ] && cmd="$cmd --target-deployment=\"$RD_OPTION_TARGET_DEPLOYMENT\""
          [ -n "$RD_OPTION_TARGET_POD" ] && cmd="$cmd --target-pod=\"$RD_OPTION_TARGET_POD\""
          [ -n "$RD_OPTION_TARGET_HELM_RELEASE" ] && cmd="$cmd --target-helm-release=\"$RD_OPTION_TARGET_HELM_RELEASE\""
        else
          _NODE_TYPE="@node.targetType@"
          _NODE_VALUE="@node.targetValue@"
          if [ -n "$_NODE_TYPE" ] && [ "$_NODE_TYPE" != "@""node.targetType""@" ]; then
            case "$_NODE_TYPE" in
              helm-release) cmd="$cmd --target-helm-release=\"$_NODE_VALUE\"" ;;
              statefulset)  cmd="$cmd --target-statefulset=\"$_NODE_VALUE\"" ;;
              deployment)   cmd="$cmd --target-deployment=\"$_NODE_VALUE\"" ;;
            esac
          fi
        fi
        _TARGET_NS="${RD_OPTION_TARGET_NAMESPACE:-@node.targetNamespace@}"
        [ -n "$_TARGET_NS" ] && [ "$_TARGET_NS" != "@""node.targetNamespace""@" ] && cmd="$cmd --target-namespace=\"$_TARGET_NS\""
        [ -n "$RD_OPTION_TARGET_LABELS" ] && cmd="$cmd --target-labels=\"$RD_OPTION_TARGET_LABELS\""
        # Use cluster URL from node attribute if available, fall back to project global
        _CLUSTER_URL="@node.clusterUrl@"
        [ -z "$_CLUSTER_URL" ] || [ "$_CLUSTER_URL" = "@""node.clusterUrl""@" ] && _CLUSTER_URL="$RD_GLOBALS_K8S_URL"
        _K8S_TOKEN="${RD_OPTION_K8S_TOKEN:-$RD_OPTION_K8S_TOKEN_DEFAULT}"
        cmd="$cmd --server=\"$_CLUSTER_URL\" --token=\"$_K8S_TOKEN\" --insecure-skip-tls-verify"
        eval $cmd
    keepgoing: false
    strategy: node-first
