# Generated by openapi-operator-gen v0.0.10-27-g49a94a0-dirty
# Apply managed PetstoreBundle "simple-bundle" (Docker execution)
- defaultTab: output
  description: |-
    Apply managed PetstoreBundle "simple-bundle" to the cluster.
    Creates the resource if it doesn't exist, updates if it does.
    Runs kubectl inside a Docker container.
    Uses: docker run ... kubectl apply -f - (with embedded CR YAML)
  executionEnabled: true
  group: managed/petstorebundle-simple-bundle
  loglevel: INFO
  name: apply-petstorebundle-simple-bundle
  nodeFilterEditable: false
  options:
  - name: namespace
    description: "Kubernetes namespace (defaults to project setting, overrides metadata.namespace)"
    value: '${globals.namespace}'
  - name: dry_run
    description: "Preview without applying"
    enforced: true
    values:
    - 'true'
    - 'false'
    value: 'false'
  - name: k8s_token
    secure: true
    valueExposed: true
    storagePath: keys/${node.clusterTokenSuffix}
    hidden: true
  - name: k8s_token_default
    secure: true
    valueExposed: true
    storagePath: keys/project/petstore-operator-docker/k8s-token
    hidden: true
  scheduleEnabled: true
  sequence:
    commands:
    - description: "Apply PetstoreBundle simple-bundle"
      scriptInterpreter: /bin/bash
      script: |-
        set -e
        IMAGE="${PLUGIN_RUNNER_IMAGE:-petstore-kubectl-plugin:latest}"
        NET="${DOCKER_NETWORK:-host}"
        cat <<'CRYAML' > /tmp/managed-cr.yaml
        apiVersion: petstore.example.com/v1alpha1
        kind: PetstoreBundle
        metadata:
          name: simple-bundle
        spec:
          resources:
            - id: pet-0
              kind: Pet
              spec:
                category: {"id": 1, "name": "Dogs"}
                name: "Fluffy"
                photoUrls: ["https://example.com/photos/fluffy.jpg"]
                status: "available"
            - id: order-1
              kind: Order
              spec:
                complete: false
                petId: ${resources.pet-0.status.externalID}
                quantity: 2
                status: "placed"
        CRYAML
        cmd="cat /tmp/managed-cr.yaml | docker run -i --rm --network $NET $IMAGE kubectl apply -f -"
        _NS="${RD_OPTION_NAMESPACE:-$RD_GLOBALS_NAMESPACE}"
        [ -n "$_NS" ] && cmd="$cmd -n $_NS"
        [ "$RD_OPTION_DRY_RUN" = "true" ] && cmd="$cmd --dry-run=client"
        # Use cluster URL from node attribute if available, fall back to project global
        _CLUSTER_URL="@node.clusterUrl@"
        [ -z "$_CLUSTER_URL" ] || [ "$_CLUSTER_URL" = "@""node.clusterUrl""@" ] && _CLUSTER_URL="$RD_GLOBALS_K8S_URL"
        _K8S_TOKEN="${RD_OPTION_K8S_TOKEN:-$RD_OPTION_K8S_TOKEN_DEFAULT}"
        cmd="$cmd --server=\"$_CLUSTER_URL\" --token=\"$_K8S_TOKEN\" --insecure-skip-tls-verify"
        eval $cmd
        rm -f /tmp/managed-cr.yaml
    keepgoing: false
    strategy: node-first
