# Generated by openapi-operator-gen v0.0.10-11-g5838925-dirty
# Status of managed PetstoreBundle "simple-bundle" (Docker execution)
- defaultTab: output
  description: |-
    Check status and health of managed PetstoreBundle "simple-bundle".
    Shows sync state, conditions, and any errors.
    Runs kubectl plugin inside a Docker container.
    Uses: docker run ... petstore describe petstorebundle simple-bundle
  executionEnabled: true
  group: managed/petstorebundle-simple-bundle
  loglevel: INFO
  name: status-petstorebundle-simple-bundle
  nodeFilterEditable: false
  options:
  - name: namespace
    description: "Kubernetes namespace (defaults to project setting)"
    value: '${globals.namespace}'
  - name: output
    description: "Output format"
    enforced: true
    values:
    - 'table'
    - 'json'
    - 'yaml'
    value: 'table'
  - name: k8s_token
    secure: true
    valueExposed: true
    storagePath: keys/project/petstore-operator-docker/k8s-token
    hidden: true
  scheduleEnabled: true
  sequence:
    commands:
    - description: "Check status of PetstoreBundle simple-bundle"
      scriptInterpreter: /bin/bash
      script: |-
        set -e
        IMAGE="${PLUGIN_RUNNER_IMAGE:-petstore-kubectl-plugin:latest}"
        NET="${DOCKER_NETWORK:-host}"
        cmd="docker run --rm --network $NET $IMAGE petstore describe petstorebundle simple-bundle"
        _NS="${RD_OPTION_NAMESPACE:-$RD_GLOBALS_NAMESPACE}"
        [ -n "$_NS" ] && cmd="$cmd -n $_NS"
        [ -n "$RD_OPTION_OUTPUT" ] && cmd="$cmd -o $RD_OPTION_OUTPUT"
        cmd="$cmd --server=\"$RD_GLOBALS_K8S_URL\" --token=\"$RD_OPTION_K8S_TOKEN\" --insecure-skip-tls-verify"
        eval $cmd
    keepgoing: false
    strategy: node-first
