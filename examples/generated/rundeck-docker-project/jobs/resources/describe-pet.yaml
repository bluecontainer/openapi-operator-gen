# Generated by openapi-operator-gen v0.0.10-11-g5838925-dirty
# Describe a Pet resource (Docker execution)
- defaultTab: output
  description: |-
    Show detailed information about a Pet resource.
    Runs kubectl plugin inside a Docker container.
    Uses: docker run ... kubectl petstore describe pet
  executionEnabled: true
  group: resources/pet
  loglevel: INFO
  name: describe-pet
  nodeFilterEditable: false
  options:
  - name: resource_name
    description: "Name of the Pet custom resource"
    required: true
  - name: namespace
    description: "Kubernetes namespace (defaults to project setting)"
    value: '${globals.namespace}'
  - name: k8s_token
    secure: true
    valueExposed: true
    storagePath: keys/project/petstore-operator-docker/k8s-token
    hidden: true
  scheduleEnabled: true
  sequence:
    commands:
    - description: "Describe Pet resource"
      scriptInterpreter: /bin/bash
      script: |-
        set -e
        IMAGE="${PLUGIN_RUNNER_IMAGE:-petstore-kubectl-plugin:latest}"
        NET="${DOCKER_NETWORK:-host}"
        cmd="docker run --rm --network $NET $IMAGE petstore describe pet $RD_OPTION_RESOURCE_NAME"
        _NS="${RD_OPTION_NAMESPACE:-$RD_GLOBALS_NAMESPACE}"
        [ -n "$_NS" ] && cmd="$cmd -n $_NS"
        cmd="$cmd --server=\"$RD_GLOBALS_K8S_URL\" --token=\"$RD_OPTION_K8S_TOKEN\" --insecure-skip-tls-verify"
        eval $cmd
    keepgoing: false
    strategy: node-first
