# Generated by openapi-operator-gen v0.0.10-8-g5ef4f69-dirty
# Create a Order resource via the Kubernetes operator (Docker execution)
- defaultTab: output
  description: |-
    Create a Order resource via the Kubernetes operator.
    Runs kubectl plugin inside a Docker container.
    Uses: docker run ... kubectl petstore create order
  executionEnabled: true
  group: resources/order
  loglevel: INFO
  name: create-order
  nodeFilterEditable: false
  options:
  - name: resource_name
    description: "Name for the CR (auto-generated if empty)"
  - name: complete
    description: ""
  - name: id
    description: ""
  - name: petId
    description: ""
  - name: quantity
    description: ""
  - name: shipDate
    description: ""
  - name: status
    description: "Order Status"
    enforced: true
    values:
    - 'placed'
    - 'approved'
    - 'delivered'
  - name: namespace
    description: "Kubernetes namespace (default: from kubeconfig)"
  - name: dry_run
    description: "Preview the resource without creating it"
    enforced: true
    values:
    - 'true'
    - 'false'
    value: 'false'
  - name: no_wait
    description: "Don't wait for the resource to sync"
    enforced: true
    values:
    - 'true'
    - 'false'
    value: 'false'
  - name: timeout
    description: "Timeout for --wait (e.g., 60s, 5m)"
    value: '60s'
  scheduleEnabled: true
  sequence:
    commands:
    - description: "Create Order resource"
      scriptInterpreter: /bin/bash
      script: |-
        set -e
        IMAGE="${PLUGIN_RUNNER_IMAGE:-petstore-kubectl-plugin:latest}"
        NET="${DOCKER_NETWORK:-host}"
        KUBE_VOL="${DOCKER_KUBE_VOLUME:-}"
        KUBE="${KUBECONFIG:-/root/.kube/config}"
        if [ -n "$KUBE_VOL" ]; then
          VOL_ARGS="-v $KUBE_VOL:/kube:ro -e KUBECONFIG=/kube/kube/config"
        else
          VOL_ARGS="-v $KUBE:/root/.kube/config:ro"
        fi
        cmd="docker run --rm --network $NET $VOL_ARGS $IMAGE petstore create order"
        [ -n "$RD_OPTION_RESOURCE_NAME" ] && cmd="$cmd --cr-name=$RD_OPTION_RESOURCE_NAME"
        cmd="$cmd --complete=\"$RD_OPTION_COMPLETE\""
        cmd="$cmd --id=\"$RD_OPTION_ID\""
        cmd="$cmd --petId=\"$RD_OPTION_PETID\""
        cmd="$cmd --quantity=\"$RD_OPTION_QUANTITY\""
        cmd="$cmd --shipDate=\"$RD_OPTION_SHIPDATE\""
        cmd="$cmd --status=\"$RD_OPTION_STATUS\""
        [ -n "$RD_OPTION_NAMESPACE" ] && cmd="$cmd -n $RD_OPTION_NAMESPACE"
        cmd="$cmd --dry-run=$RD_OPTION_DRY_RUN --timeout=$RD_OPTION_TIMEOUT"
        [ "$RD_OPTION_NO_WAIT" = "true" ] && cmd="$cmd --no-wait"
        eval $cmd
    keepgoing: false
    strategy: node-first
