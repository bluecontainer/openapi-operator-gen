# Generated by openapi-operator-gen v0.0.10-10-ge5a6a04-dirty
# Create a Pet resource via the Kubernetes operator (Docker execution)
- defaultTab: output
  description: |-
    Create a Pet resource via the Kubernetes operator.
    Runs kubectl plugin inside a Docker container.
    Uses: docker run ... kubectl petstore create pet
  executionEnabled: true
  group: resources/pet
  loglevel: INFO
  name: create-pet
  nodeFilterEditable: false
  options:
  - name: resource_name
    description: "Name for the CR (auto-generated if empty)"
  - name: category
    description: " (JSON format)"
  - name: id
    description: ""
  - name: name
    description: ""
  - name: photoUrls
    description: " (JSON format)"
  - name: status
    description: "pet status in the store"
    enforced: true
    values:
    - 'available'
    - 'pending'
    - 'sold'
  - name: tags
    description: " (JSON format)"
  - name: namespace
    description: "Kubernetes namespace"
  - name: dry_run
    description: "Preview the resource without creating it"
    enforced: true
    values:
    - 'true'
    - 'false'
    value: 'false'
  - name: no_wait
    description: "Don't wait for the resource to sync"
    enforced: true
    values:
    - 'true'
    - 'false'
    value: 'false'
  - name: timeout
    description: "Timeout for --wait (e.g., 60s, 5m)"
    value: '60s'
  - name: k8s_token
    secure: true
    valueExposed: true
    storagePath: keys/project/petstore-operator-docker/k8s-token
    hidden: true
  scheduleEnabled: true
  sequence:
    commands:
    - description: "Create Pet resource"
      scriptInterpreter: /bin/bash
      script: |-
        set -e
        IMAGE="${PLUGIN_RUNNER_IMAGE:-petstore-kubectl-plugin:latest}"
        NET="${DOCKER_NETWORK:-host}"
        cmd="docker run --rm --network $NET $IMAGE petstore create pet"
        [ -n "$RD_OPTION_RESOURCE_NAME" ] && cmd="$cmd --cr-name=$RD_OPTION_RESOURCE_NAME"
        cmd="$cmd --category=\"$RD_OPTION_CATEGORY\""
        cmd="$cmd --id=\"$RD_OPTION_ID\""
        cmd="$cmd --name=\"$RD_OPTION_NAME\""
        cmd="$cmd --photoUrls=\"$RD_OPTION_PHOTOURLS\""
        cmd="$cmd --status=\"$RD_OPTION_STATUS\""
        cmd="$cmd --tags=\"$RD_OPTION_TAGS\""
        [ -n "$RD_OPTION_NAMESPACE" ] && cmd="$cmd -n $RD_OPTION_NAMESPACE"
        cmd="$cmd --dry-run=$RD_OPTION_DRY_RUN --timeout=$RD_OPTION_TIMEOUT"
        [ "$RD_OPTION_NO_WAIT" = "true" ] && cmd="$cmd --no-wait"
        cmd="$cmd --server=\"$RD_GLOBALS_K8S_URL\" --token=\"$RD_OPTION_K8S_TOKEN\" --insecure-skip-tls-verify"
        eval $cmd
    keepgoing: false
    strategy: node-first
