# Generated by openapi-operator-gen v0.0.10-10-ge5a6a04-dirty
# Describe a User resource (Docker execution)
- defaultTab: output
  description: |-
    Show detailed information about a User resource.
    Runs kubectl plugin inside a Docker container.
    Uses: docker run ... kubectl petstore describe user
  executionEnabled: true
  group: resources/user
  loglevel: INFO
  name: describe-user
  nodeFilterEditable: false
  options:
  - name: resource_name
    description: "Name of the User custom resource"
    required: true
  - name: namespace
    description: "Kubernetes namespace"
  - name: k8s_token
    secure: true
    valueExposed: true
    storagePath: keys/project/petstore-operator-docker/k8s-token
    hidden: true
  scheduleEnabled: true
  sequence:
    commands:
    - description: "Describe User resource"
      scriptInterpreter: /bin/bash
      script: |-
        set -e
        IMAGE="${PLUGIN_RUNNER_IMAGE:-petstore-kubectl-plugin:latest}"
        NET="${DOCKER_NETWORK:-host}"
        cmd="docker run --rm --network $NET $IMAGE petstore describe user $RD_OPTION_RESOURCE_NAME"
        [ -n "$RD_OPTION_NAMESPACE" ] && cmd="$cmd -n $RD_OPTION_NAMESPACE"
        cmd="$cmd --server=\"$RD_GLOBALS_K8S_URL\" --token=\"$RD_OPTION_K8S_TOKEN\" --insecure-skip-tls-verify"
        eval $cmd
    keepgoing: false
    strategy: node-first
