# Generated by openapi-operator-gen v0.0.10-10-ge5a6a04-dirty
# List Order resources (Docker execution)
- defaultTab: output
  description: |-
    List Order resources managed by the Kubernetes operator.
    Runs kubectl plugin inside a Docker container.
    Uses: docker run ... kubectl petstore get orders
  executionEnabled: true
  group: resources/order
  loglevel: INFO
  name: get-orders
  nodeFilterEditable: false
  options:
  - name: namespace
    description: "Kubernetes namespace"
  - name: output
    description: "Output format"
    enforced: true
    values:
    - 'table'
    - 'wide'
    - 'json'
    - 'yaml'
    value: 'table'
  - name: k8s_token
    secure: true
    valueExposed: true
    storagePath: keys/project/petstore-operator-docker/k8s-token
    hidden: true
  scheduleEnabled: true
  sequence:
    commands:
    - description: "List Order resources"
      scriptInterpreter: /bin/bash
      script: |-
        set -e
        IMAGE="${PLUGIN_RUNNER_IMAGE:-petstore-kubectl-plugin:latest}"
        NET="${DOCKER_NETWORK:-host}"
        cmd="docker run --rm --network $NET $IMAGE petstore get orders"
        [ -n "$RD_OPTION_NAMESPACE" ] && cmd="$cmd -n $RD_OPTION_NAMESPACE"
        [ -n "$RD_OPTION_OUTPUT" ] && cmd="$cmd -o $RD_OPTION_OUTPUT"
        cmd="$cmd --server=\"$RD_GLOBALS_K8S_URL\" --token=\"$RD_OPTION_K8S_TOKEN\" --insecure-skip-tls-verify"
        eval $cmd
    keepgoing: false
    strategy: node-first
