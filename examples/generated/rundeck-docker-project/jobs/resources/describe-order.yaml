# Generated by openapi-operator-gen v0.0.10-7-g3ef1d52-dirty
# Describe a Order resource (Docker execution)
- defaultTab: output
  description: |-
    Show detailed information about a Order resource.
    Runs kubectl plugin inside a Docker container.
    Uses: docker run ... kubectl petstore describe order
  executionEnabled: true
  group: resources/order
  loglevel: INFO
  name: describe-order
  nodeFilterEditable: false
  options:
  - name: resource_name
    description: "Name of the Order custom resource"
    required: true
  - name: namespace
    description: "Kubernetes namespace (default: from kubeconfig)"
  scheduleEnabled: true
  sequence:
    commands:
    - description: "Describe Order resource"
      scriptInterpreter: /bin/bash
      script: |-
        set -e
        IMAGE="${PLUGIN_RUNNER_IMAGE:-petstore-kubectl-plugin:latest}"
        NET="${DOCKER_NETWORK:-host}"
        KUBE_VOL="${DOCKER_KUBE_VOLUME:-}"
        KUBE="${KUBECONFIG:-/root/.kube/config}"
        if [ -n "$KUBE_VOL" ]; then
          VOL_ARGS="-v $KUBE_VOL:/kube:ro -e KUBECONFIG=/kube/kube/config"
        else
          VOL_ARGS="-v $KUBE:/root/.kube/config:ro"
        fi
        cmd="docker run --rm --network $NET $VOL_ARGS $IMAGE petstore describe order $RD_OPTION_RESOURCE_NAME"
        [ -n "$RD_OPTION_NAMESPACE" ] && cmd="$cmd -n $RD_OPTION_NAMESPACE"
        eval $cmd
    keepgoing: false
    strategy: node-first
