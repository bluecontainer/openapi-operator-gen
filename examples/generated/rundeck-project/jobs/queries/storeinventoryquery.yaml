# Generated by openapi-operator-gen v0.0.10-7-g3ef1d52-dirty
# Execute StoreInventoryQuery query
- defaultTab: output
  description: |-
    Execute a StoreInventoryQuery query via the Kubernetes operator.
    Uses: kubectl petstore query storeinventoryquery
  executionEnabled: true
  group: queries
  loglevel: INFO
  name: storeinventoryquery
  nodeFilterEditable: false
  options:
  - name: resource_name
    description: "Name for the query CR (auto-generated if empty)"
  - name: namespace
    description: "Kubernetes namespace (default: from kubeconfig)"
  - name: dry_run
    description: "Preview the query resource without creating it"
    enforced: true
    values:
    - 'true'
    - 'false'
    value: 'false'
  - name: wait
    description: "Wait for query results"
    enforced: true
    values:
    - 'true'
    - 'false'
    value: 'true'
  - name: timeout
    description: "Timeout for --wait (e.g., 60s, 5m)"
    value: '60s'
  scheduleEnabled: true
  sequence:
    commands:
    - description: "Execute StoreInventoryQuery query"
      scriptInterpreter: /bin/bash
      script: |-
        set -e
        cmd="kubectl petstore query storeinventoryquery"
        [ -n "$RD_OPTION_RESOURCE_NAME" ] && cmd="$cmd --name=$RD_OPTION_RESOURCE_NAME"
        [ -n "$RD_OPTION_NAMESPACE" ] && cmd="$cmd -n $RD_OPTION_NAMESPACE"
        cmd="$cmd --dry-run=$RD_OPTION_DRY_RUN --wait=$RD_OPTION_WAIT --timeout=$RD_OPTION_TIMEOUT"
        eval $cmd
    keepgoing: false
    strategy: node-first
