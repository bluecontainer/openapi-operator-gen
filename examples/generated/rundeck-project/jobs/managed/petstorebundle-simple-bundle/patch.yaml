# Generated by openapi-operator-gen v0.0.10-13-g20d778f-dirty
# Patch managed PetstoreBundle "simple-bundle" (script execution)
- defaultTab: output
  description: |-
    Patch managed PetstoreBundle "simple-bundle" with a JSON merge patch.
    Example patch: {"spec":{"paused":true}}
    Uses: kubectl patch petstorebundle simple-bundle --type=merge -p '...'
  executionEnabled: true
  group: managed/petstorebundle-simple-bundle
  loglevel: INFO
  name: patch-petstorebundle-simple-bundle
  nodeFilterEditable: false
  options:
  - name: patch_json
    description: "JSON merge patch (e.g., {\"spec\":{\"paused\":true}})"
    required: true
  - name: namespace
    description: "Kubernetes namespace (defaults to project setting)"
    value: '${globals.namespace}'
  - name: dry_run
    description: "Preview without applying"
    enforced: true
    values:
    - 'true'
    - 'false'
    value: 'false'
  - name: k8s_token
    secure: true
    valueExposed: true
    storagePath: keys/project/petstore-operator/k8s-token
    hidden: true
  scheduleEnabled: true
  sequence:
    commands:
    - description: "Patch PetstoreBundle simple-bundle"
      scriptInterpreter: /bin/bash
      script: |-
        set -e
        cmd="kubectl patch petstorebundle simple-bundle --type=merge -p '$RD_OPTION_PATCH_JSON'"
        _NS="${RD_OPTION_NAMESPACE:-$RD_GLOBALS_NAMESPACE}"
        [ -n "$_NS" ] && cmd="$cmd -n $_NS"
        [ "$RD_OPTION_DRY_RUN" = "true" ] && cmd="$cmd --dry-run=client"
        cmd="$cmd --server=\"$RD_GLOBALS_K8S_URL\" --token=\"$RD_OPTION_K8S_TOKEN\" --insecure-skip-tls-verify"
        eval $cmd
    keepgoing: false
    strategy: node-first
