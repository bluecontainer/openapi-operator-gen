# Generated by openapi-operator-gen v0.0.10-8-g5ef4f69-dirty
# List Order resources
- defaultTab: output
  description: |-
    List Order resources managed by the Kubernetes operator.
    Uses: kubectl petstore get orders
  executionEnabled: true
  group: resources/order
  loglevel: INFO
  name: get-orders
  nodeFilterEditable: false
  options:
  - name: namespace
    description: "Kubernetes namespace (default: from kubeconfig)"
  - name: output
    description: "Output format"
    enforced: true
    values:
    - 'table'
    - 'wide'
    - 'json'
    - 'yaml'
    value: 'table'
  scheduleEnabled: true
  sequence:
    commands:
    - description: "List Order resources"
      scriptInterpreter: /bin/bash
      script: |-
        set -e
        cmd="kubectl petstore get orders"
        [ -n "$RD_OPTION_NAMESPACE" ] && cmd="$cmd -n $RD_OPTION_NAMESPACE"
        [ -n "$RD_OPTION_OUTPUT" ] && cmd="$cmd -o $RD_OPTION_OUTPUT"
        eval $cmd
    keepgoing: false
    strategy: node-first
