# Generated by openapi-operator-gen v0.0.10-9-ge9f872c-dirty
# Create a Order resource via the Kubernetes operator
- defaultTab: output
  description: |-
    Create a Order resource via the Kubernetes operator.
    Uses: kubectl petstore create order
  executionEnabled: true
  group: resources/order
  loglevel: INFO
  name: create-order
  nodeFilterEditable: false
  options:
  - name: resource_name
    description: "Name for the CR (auto-generated if empty)"
  - name: complete
    description: ""
  - name: id
    description: ""
  - name: petId
    description: ""
  - name: quantity
    description: ""
  - name: shipDate
    description: ""
  - name: status
    description: "Order Status"
    enforced: true
    values:
    - 'placed'
    - 'approved'
    - 'delivered'
  - name: namespace
    description: "Kubernetes namespace"
  - name: dry_run
    description: "Preview the resource without creating it"
    enforced: true
    values:
    - 'true'
    - 'false'
    value: 'false'
  - name: no_wait
    description: "Don't wait for the resource to sync"
    enforced: true
    values:
    - 'true'
    - 'false'
    value: 'false'
  - name: timeout
    description: "Timeout for --wait (e.g., 60s, 5m)"
    value: '60s'
  - name: k8s_token
    secure: true
    valueExposed: true
    storagePath: keys/project/petstore-operator/k8s-token
    hidden: true
  - name: k8s_url
    secure: true
    valueExposed: true
    storagePath: keys/project/petstore-operator/k8s-url
    hidden: true
  scheduleEnabled: true
  sequence:
    commands:
    - description: "Create Order resource"
      scriptInterpreter: /bin/bash
      script: |-
        set -e
        cmd="kubectl petstore create order"
        [ -n "$RD_OPTION_RESOURCE_NAME" ] && cmd="$cmd --cr-name=$RD_OPTION_RESOURCE_NAME"
        cmd="$cmd --complete=\"$RD_OPTION_COMPLETE\""
        cmd="$cmd --id=\"$RD_OPTION_ID\""
        cmd="$cmd --petId=\"$RD_OPTION_PETID\""
        cmd="$cmd --quantity=\"$RD_OPTION_QUANTITY\""
        cmd="$cmd --shipDate=\"$RD_OPTION_SHIPDATE\""
        cmd="$cmd --status=\"$RD_OPTION_STATUS\""
        [ -n "$RD_OPTION_NAMESPACE" ] && cmd="$cmd -n $RD_OPTION_NAMESPACE"
        cmd="$cmd --dry-run=$RD_OPTION_DRY_RUN --timeout=$RD_OPTION_TIMEOUT"
        [ "$RD_OPTION_NO_WAIT" = "true" ] && cmd="$cmd --no-wait"
        cmd="$cmd --server=\"$RD_OPTION_K8S_URL\" --token=\"$RD_OPTION_K8S_TOKEN\" --insecure-skip-tls-verify"
        eval $cmd
    keepgoing: false
    strategy: node-first
