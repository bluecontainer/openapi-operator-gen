# Generated by openapi-operator-gen v0.0.10-7-g3ef1d52-dirty
# Execute PetUploadimageAction action
- defaultTab: output
  description: |-
    Execute a PetUploadimageAction action via the Kubernetes operator.
    Uses: kubectl petstore action petuploadimageaction
  executionEnabled: true
  group: actions
  loglevel: INFO
  name: petuploadimageaction
  nodeFilterEditable: false
  options:
  - name: resource_name
    description: "Name for the action CR (auto-generated if empty)"
  - name: petId
    description: "ID of the parent Pet resource"
    required: true
  - name: additionalMetadata
    description: "Additional Metadata"
  - name: namespace
    description: "Kubernetes namespace (default: from kubeconfig)"
  - name: dry_run
    description: "Preview the action resource without creating it"
    enforced: true
    values:
    - 'true'
    - 'false'
    value: 'false'
  - name: wait
    description: "Wait for action completion"
    enforced: true
    values:
    - 'true'
    - 'false'
    value: 'true'
  - name: timeout
    description: "Timeout for --wait (e.g., 60s, 5m)"
    value: '60s'
  scheduleEnabled: true
  sequence:
    commands:
    - description: "Execute PetUploadimageAction action"
      scriptInterpreter: /bin/bash
      script: |-
        set -e
        cmd="kubectl petstore action petuploadimageaction"
        [ -n "$RD_OPTION_RESOURCE_NAME" ] && cmd="$cmd --name=$RD_OPTION_RESOURCE_NAME"
        cmd="$cmd --petId=\"$RD_OPTION_PETID\""
        cmd="$cmd --additionalMetadata=\"$RD_OPTION_ADDITIONALMETADATA\""
        [ -n "$RD_OPTION_NAMESPACE" ] && cmd="$cmd -n $RD_OPTION_NAMESPACE"
        cmd="$cmd --dry-run=$RD_OPTION_DRY_RUN --wait=$RD_OPTION_WAIT --timeout=$RD_OPTION_TIMEOUT"
        eval $cmd
    keepgoing: false
    strategy: node-first
