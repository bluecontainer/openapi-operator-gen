---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: petfindbystatusqueries.petstore.example.com
spec:
  group: petstore.example.com
  names:
    kind: PetFindbystatusQuery
    listKind: PetFindbystatusQueryList
    plural: petfindbystatusqueries
    singular: petfindbystatusquery
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.state
      name: State
      type: string
    - jsonPath: .status.resultCount
      name: Results
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PetFindbystatusQuery is the Schema for the petfindbystatusqueries
          API (Query Operation)
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PetFindbystatusQuerySpec defines the query parameters for
              PetFindbystatusQuery
            properties:
              executionInterval:
                description: |-
                  ExecutionInterval specifies how often to re-execute the query.
                  If not set, the query executes once and stores results (one-shot mode).
                  Examples: "30s", "5m", "1h"
                type: string
              paused:
                description: |-
                  Paused stops reconciliation when true.
                  Useful for debugging or temporary suspension of query execution.
                type: boolean
              status:
                description: Status values that need to be considered for filter
                type: string
              target:
                description: |-
                  Target specifies endpoint targeting configuration.
                  If not specified, the operator uses its global configuration.
                properties:
                  baseURL:
                    description: |-
                      BaseURL specifies a static base URL for the REST API.
                      When set, overrides all other endpoint configuration.
                      Example: "http://api.example.com:8080"
                      Mutually exclusive with BaseURLs.
                    type: string
                  baseURLs:
                    description: |-
                      BaseURLs specifies multiple static base URLs for the REST API.
                      Operations will fan-out to all URLs (writes must succeed on all, reads use first success).
                      Example: ["http://api-1.example.com:8080", "http://api-2.example.com:8080"]
                      Mutually exclusive with BaseURL.
                    items:
                      type: string
                    type: array
                  deployment:
                    description: |-
                      Deployment specifies a Deployment name to route requests to.
                      When combined with HelmRelease, narrows discovery to only this Deployment within the release.
                    type: string
                  helmRelease:
                    description: |-
                      HelmRelease specifies a Helm release to discover the workload from.
                      The operator will find StatefulSets or Deployments with label app.kubernetes.io/instance=<HelmRelease>.
                    type: string
                  labels:
                    additionalProperties:
                      type: string
                    description: |-
                      Labels specifies additional labels to match when discovering workloads.
                      When combined with HelmRelease, narrows discovery to workloads matching these labels
                      in addition to the Helm release instance label.
                    type: object
                  namespace:
                    description: |-
                      Namespace specifies the namespace to look for the target workload.
                      Defaults to the CR's namespace if not specified.
                    type: string
                  pod:
                    description: Pod specifies a pod name to route requests to directly.
                    type: string
                  podOrdinal:
                    description: |-
                      PodOrdinal specifies the StatefulSet pod ordinal to route requests to.
                      Only used when targeting a StatefulSet and the operator is configured with --strategy=by-ordinal.
                    format: int32
                    minimum: 0
                    type: integer
                  statefulSet:
                    description: |-
                      StatefulSet specifies a StatefulSet name to route requests to.
                      When combined with HelmRelease, narrows discovery to only this StatefulSet within the release.
                    type: string
                type: object
            required:
            - status
            type: object
          status:
            description: PetFindbystatusQueryStatus defines the observed state of
              PetFindbystatusQuery
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of an object's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              executionCount:
                description: ExecutionCount is the number of times this query has
                  been executed
                format: int64
                type: integer
              lastExecutionTime:
                description: LastExecutionTime is the last time the query was executed
                  (alias for re-execution support)
                format: date-time
                type: string
              lastQueryTime:
                description: LastQueryTime is the last time the query was executed
                format: date-time
                type: string
              message:
                description: Message is a human-readable message about the current
                  state
                type: string
              nextExecutionTime:
                description: NextExecutionTime is when the next periodic execution
                  will occur
                format: date-time
                type: string
              observedGeneration:
                description: ObservedGeneration is the last observed generation
                format: int64
                type: integer
              responses:
                additionalProperties:
                  description: PetFindbystatusQueryEndpointResponse contains the response
                    from a single endpoint for PetFindbystatusQuery queries
                  properties:
                    data:
                      description: Data contains the response body from the endpoint
                      items:
                        description: PetFindbystatusQueryResult represents a single
                          result item from the PetFindbystatusQuery query
                        properties:
                          category:
                            description: PetFindbystatusQueryResultCategory is a nested
                              type used by CRD specs
                            properties:
                              id:
                                format: int64
                                type: integer
                              name:
                                type: string
                            type: object
                          id:
                            format: int64
                            type: integer
                          name:
                            type: string
                          photoUrls:
                            items:
                              type: string
                            type: array
                          status:
                            description: pet status in the store
                            type: string
                          tags:
                            items:
                              description: PetFindbystatusQueryResultTagsItem is a
                                nested type used by CRD specs
                              properties:
                                id:
                                  format: int64
                                  type: integer
                                name:
                                  type: string
                              type: object
                            type: array
                        type: object
                      type: array
                    error:
                      description: Error contains the error message if the request
                        failed
                      type: string
                    lastUpdated:
                      description: LastUpdated is when this endpoint was last queried
                      format: date-time
                      type: string
                    statusCode:
                      description: StatusCode is the HTTP status code returned by
                        the endpoint
                      type: integer
                    success:
                      description: Success indicates whether the request to this endpoint
                        succeeded
                      type: boolean
                  required:
                  - success
                  type: object
                description: Responses contains responses from multiple endpoints
                  (all-healthy strategy)
                type: object
              resultCount:
                description: ResultCount is the number of results returned by the
                  query
                type: integer
              results:
                description: Results contains the query result from the REST API (single
                  endpoint mode)
                properties:
                  data:
                    description: Data contains the response body from the endpoint
                    items:
                      description: PetFindbystatusQueryResult represents a single
                        result item from the PetFindbystatusQuery query
                      properties:
                        category:
                          description: PetFindbystatusQueryResultCategory is a nested
                            type used by CRD specs
                          properties:
                            id:
                              format: int64
                              type: integer
                            name:
                              type: string
                          type: object
                        id:
                          format: int64
                          type: integer
                        name:
                          type: string
                        photoUrls:
                          items:
                            type: string
                          type: array
                        status:
                          description: pet status in the store
                          type: string
                        tags:
                          items:
                            description: PetFindbystatusQueryResultTagsItem is a nested
                              type used by CRD specs
                            properties:
                              id:
                                format: int64
                                type: integer
                              name:
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  error:
                    description: Error contains the error message if the request failed
                    type: string
                  lastUpdated:
                    description: LastUpdated is when this endpoint was last queried
                    format: date-time
                    type: string
                  statusCode:
                    description: StatusCode is the HTTP status code returned by the
                      endpoint
                    type: integer
                  success:
                    description: Success indicates whether the request to this endpoint
                      succeeded
                    type: boolean
                required:
                - success
                type: object
              state:
                description: State represents the current state of the query
                enum:
                - Pending
                - Querying
                - Queried
                - Failed
                - Paused
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
