---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: petuploadimageactions.petstore.example.com
spec:
  group: petstore.example.com
  names:
    kind: PetUploadimageAction
    listKind: PetUploadimageActionList
    plural: petuploadimageactions
    singular: petuploadimageaction
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.state
      name: State
      type: string
    - jsonPath: .status.httpStatusCode
      name: HTTP Status
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PetUploadimageAction is the Schema for the petuploadimageactions
          API (Action Operation)
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PetUploadimageActionSpec defines the parameters for the PetUploadimageAction
              action
            properties:
              additionalMetadata:
                description: Additional Metadata
                type: string
              petId:
                description: ID of the parent Pet resource
                type: string
              reExecuteInterval:
                description: Interval at which to re-execute the action (e.g., 30s,
                  5m, 1h). If not set, action is one-shot.
                type: string
              targetBaseURL:
                description: |-
                  TargetBaseURL specifies a static base URL for the REST API.
                  When set, overrides all other endpoint configuration for this CR.
                  Example: "http://api.example.com:8080"
                type: string
              targetDeployment:
                description: TargetDeployment specifies a Deployment name to route
                  requests to.
                type: string
              targetHelmRelease:
                description: TargetHelmRelease specifies a Helm release to discover
                  the workload from.
                type: string
              targetNamespace:
                description: TargetNamespace specifies the namespace to look for the
                  target workload.
                type: string
              targetPod:
                description: TargetPod specifies a pod name to route requests to directly.
                type: string
              targetPodOrdinal:
                description: TargetPodOrdinal specifies the StatefulSet pod ordinal
                  to route requests to.
                format: int32
                minimum: 0
                type: integer
              targetStatefulSet:
                description: TargetStatefulSet specifies a StatefulSet name to route
                  requests to.
                type: string
            required:
            - petId
            type: object
          status:
            description: PetUploadimageActionStatus defines the observed state of
              PetUploadimageAction
            properties:
              completedAt:
                description: CompletedAt is when the action completed (success or
                  failure)
                format: date-time
                type: string
              conditions:
                description: Conditions represent the latest available observations
                  of an object's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              executedAt:
                description: ExecutedAt is when the action was executed
                format: date-time
                type: string
              executionCount:
                description: ExecutionCount is the number of times this action has
                  been executed
                format: int64
                type: integer
              httpStatusCode:
                description: HTTPStatusCode is the HTTP status code from the action
                  response (single endpoint mode)
                type: integer
              lastExecutionTime:
                description: LastExecutionTime is the last time the action was executed
                  (for re-execution support)
                format: date-time
                type: string
              message:
                description: Message is a human-readable message about the current
                  state
                type: string
              nextExecutionTime:
                description: NextExecutionTime is when the next periodic execution
                  will occur
                format: date-time
                type: string
              observedGeneration:
                description: ObservedGeneration is the last observed generation
                format: int64
                type: integer
              responses:
                additionalProperties:
                  description: PetUploadimageActionEndpointResponse contains the response
                    from a single endpoint for PetUploadimageAction actions
                  properties:
                    data:
                      description: Data contains the response body from the endpoint
                      properties:
                        code:
                          format: int32
                          type: integer
                        message:
                          type: string
                        type:
                          type: string
                      type: object
                    error:
                      description: Error contains the error message if the request
                        failed
                      type: string
                    executedAt:
                      description: ExecutedAt is when this endpoint was called
                      format: date-time
                      type: string
                    statusCode:
                      description: StatusCode is the HTTP status code returned by
                        the endpoint
                      type: integer
                    success:
                      description: Success indicates whether the request to this endpoint
                        succeeded
                      type: boolean
                  required:
                  - success
                  type: object
                description: |-
                  Responses contains responses from multiple endpoints (all-healthy strategy)
                  Keys are endpoint URLs, values are the response data
                type: object
              result:
                description: Result contains the response from the action execution
                  (single endpoint mode)
                properties:
                  data:
                    description: Data contains the response body from the endpoint
                    properties:
                      code:
                        format: int32
                        type: integer
                      message:
                        type: string
                      type:
                        type: string
                    type: object
                  error:
                    description: Error contains the error message if the request failed
                    type: string
                  executedAt:
                    description: ExecutedAt is when this endpoint was called
                    format: date-time
                    type: string
                  statusCode:
                    description: StatusCode is the HTTP status code returned by the
                      endpoint
                    type: integer
                  success:
                    description: Success indicates whether the request to this endpoint
                      succeeded
                    type: boolean
                required:
                - success
                type: object
              state:
                description: State represents the current state of the action
                enum:
                - Pending
                - Executing
                - Completed
                - Failed
                type: string
              successCount:
                description: SuccessCount is the number of endpoints that executed
                  successfully (all-healthy strategy)
                type: integer
              totalEndpoints:
                description: TotalEndpoints is the total number of endpoints targeted
                  (all-healthy strategy)
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
