# Generated by openapi-operator-gen v0.0.10-13-g20d778f-dirty
apiVersion: apps/v1
kind: Deployment
metadata:
  name: controller-manager
  namespace: petstore-system
  labels:
    app.kubernetes.io/name: petstore
    app.kubernetes.io/managed-by: openapi-operator-gen
    control-plane: controller-manager
spec:
  replicas: 1
  selector:
    matchLabels:
      control-plane: controller-manager
  template:
    metadata:
      labels:
        control-plane: controller-manager
      annotations:
        kubectl.kubernetes.io/default-container: manager
    spec:
      serviceAccountName: controller-manager
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
      - name: manager
        image: controller:latest  # Replaced by kustomize
        imagePullPolicy: IfNotPresent
        command:
        - /manager
        args:
        - --leader-elect
        env:
        # - name: REST_API_BASE_URL
        #   value: "http://api-server:8080"  # TODO: Configure your API base URL
        # OpenTelemetry configuration (optional)
        # Uncomment and configure to enable tracing and metrics
        # - name: OTEL_EXPORTER_OTLP_ENDPOINT
        #   value: "otel-collector:4317"
        # - name: OTEL_SERVICE_NAME
        #   value: "petstore-operator"
        # - name: OTEL_INSECURE
        #   value: "true"
        # Kubernetes metadata for telemetry
        - name: POD_NAME
          valueFrom:
            fieldRef:
              fieldPath: metadata.name
        - name: POD_NAMESPACE
          valueFrom:
            fieldRef:
              fieldPath: metadata.namespace
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8081
          initialDelaySeconds: 15
          periodSeconds: 20
        readinessProbe:
          httpGet:
            path: /readyz
            port: 8081
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          limits:
            cpu: 500m
            memory: 128Mi
          requests:
            cpu: 10m
            memory: 64Mi
      terminationGracePeriodSeconds: 10
