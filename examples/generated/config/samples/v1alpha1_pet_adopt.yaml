# Generated by openapi-operator-gen v0.0.10-21-g475b898-dirty
# Example Pet - Adopt and modify an existing resource
#
# This CR demonstrates adopting an existing resource from the REST API
# and changing one of its fields. When deleted, the resource will be
# restored to its original state (onDelete: Restore).
#
# Use case: Temporarily modify an existing resource under Kubernetes control
apiVersion: petstore.example.com/v1alpha1
kind: Pet
metadata:
  name: pet-adopt-and-modify
  namespace: default
spec:
  id: 10
  name: "Fluffy (Modified)"

  # Restore original state when this CR is deleted
  onDelete: "Restore"

  # Endpoint targeting (optional - uncomment if needed)
  # target:
  #   namespace: "target-namespace"
  #   statefulSet: "my-statefulset"
  #   deployment: "my-deployment"
  #   helmRelease: "my-release"
  #   podOrdinal: 0
  #   # Single static URL:
  #   # baseURL: "http://api.example.com:8080"
  #   # Multiple static URLs (fan-out to all):
  #   # baseURLs:
  #   #   - "http://api-1.example.com:8080"
  #   #   - "http://api-2.example.com:8080"
