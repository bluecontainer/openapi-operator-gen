# Generated by openapi-operator-gen v0.0.10-11-g5838925-dirty
# Diagnose a resource (Kubernetes pod execution)
- defaultTab: output
  description: |-
    Run diagnostics on a specific resource.
    Checks controller health, API connectivity, and resource state.
    Runs kubectl plugin inside an ephemeral Kubernetes pod.
    Uses: kubectl run ... -- petstore diagnose {kind} {name}
  executionEnabled: true
  group: operations
  loglevel: INFO
  name: diagnose
  nodeFilterEditable: false
  options:
  - name: resource_kind
    description: "Resource kind (e.g., Pet, Order, User)"
    required: true
  - name: resource_name
    description: "Resource name"
    required: true
  - name: pod
    description: "Specific pod name to check"
  - name: check_latency
    description: "Check API endpoint latency"
    enforced: true
    values:
    - 'true'
    - 'false'
    value: 'false'
  - name: verbose
    description: "Show detailed diagnostic output"
    enforced: true
    values:
    - 'true'
    - 'false'
    value: 'false'
  - name: namespace
    description: "Kubernetes namespace (defaults to project setting)"
    value: '${globals.namespace}'
  - name: k8s_token
    secure: true
    valueExposed: true
    storagePath: keys/project/petstore-operator-k8s/k8s-token
    hidden: true
  scheduleEnabled: true
  sequence:
    commands:
    - description: "Run resource diagnostics"
      scriptInterpreter: /bin/bash
      script: |-
        set -e
        IMAGE="${PLUGIN_RUNNER_IMAGE:-petstore-kubectl-plugin:latest}"
        PLUGIN_NS="${PLUGIN_NAMESPACE:-petstore-system}"
        SA="${PLUGIN_SERVICE_ACCOUNT:-petstore-plugin-runner}"
        POD="plugin-run-$(date +%s)-$RANDOM"
        cmd="kubectl --server=\"$RD_GLOBALS_K8S_URL\" --token=\"$RD_OPTION_K8S_TOKEN\" --insecure-skip-tls-verify"
        cmd="$cmd run $POD --image=$IMAGE --restart=Never --rm -i --quiet --image-pull-policy=Never"
        cmd="$cmd -n $PLUGIN_NS"
        cmd="$cmd --overrides='{\"spec\":{\"serviceAccountName\":\"$SA\"}}'"
        cmd="$cmd -- petstore diagnose $RD_OPTION_RESOURCE_KIND $RD_OPTION_RESOURCE_NAME"
        [ -n "$RD_OPTION_POD" ] && cmd="$cmd --pod=$RD_OPTION_POD"
        [ "$RD_OPTION_CHECK_LATENCY" = "true" ] && cmd="$cmd --check-latency"
        [ "$RD_OPTION_VERBOSE" = "true" ] && cmd="$cmd --verbose"
        _NS="${RD_OPTION_NAMESPACE:-$RD_GLOBALS_NAMESPACE}"
        [ -n "$_NS" ] && cmd="$cmd -n $_NS"
        eval $cmd
    keepgoing: false
    strategy: node-first
