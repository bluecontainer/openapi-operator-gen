# Generated by openapi-operator-gen v0.0.10-10-ge5a6a04-dirty
# Create a User resource via the Kubernetes operator (Kubernetes pod execution)
- defaultTab: output
  description: |-
    Create a User resource via the Kubernetes operator.
    Runs kubectl plugin inside an ephemeral Kubernetes pod.
    Uses: kubectl run ... -- petstore create user
  executionEnabled: true
  group: resources/user
  loglevel: INFO
  name: create-user
  nodeFilterEditable: false
  options:
  - name: resource_name
    description: "Name for the CR (auto-generated if empty)"
  - name: email
    description: ""
  - name: firstName
    description: ""
  - name: id
    description: ""
  - name: lastName
    description: ""
  - name: password
    description: ""
  - name: phone
    description: ""
  - name: userStatus
    description: "User Status"
  - name: username
    description: ""
  - name: namespace
    description: "Kubernetes namespace"
  - name: dry_run
    description: "Preview the resource without creating it"
    enforced: true
    values:
    - 'true'
    - 'false'
    value: 'false'
  - name: no_wait
    description: "Don't wait for the resource to sync"
    enforced: true
    values:
    - 'true'
    - 'false'
    value: 'false'
  - name: timeout
    description: "Timeout for --wait (e.g., 60s, 5m)"
    value: '60s'
  - name: k8s_token
    secure: true
    valueExposed: true
    storagePath: keys/project/petstore-operator-k8s/k8s-token
    hidden: true
  scheduleEnabled: true
  sequence:
    commands:
    - description: "Create User resource"
      scriptInterpreter: /bin/bash
      script: |-
        set -e
        IMAGE="${PLUGIN_RUNNER_IMAGE:-petstore-kubectl-plugin:latest}"
        PLUGIN_NS="${PLUGIN_NAMESPACE:-petstore-system}"
        SA="${PLUGIN_SERVICE_ACCOUNT:-petstore-plugin-runner}"
        POD="plugin-run-$(date +%s)-$RANDOM"
        cmd="kubectl --server=\"$RD_GLOBALS_K8S_URL\" --token=\"$RD_OPTION_K8S_TOKEN\" --insecure-skip-tls-verify"
        cmd="$cmd run $POD --image=$IMAGE --restart=Never --rm -i --quiet --image-pull-policy=Never"
        cmd="$cmd -n $PLUGIN_NS"
        cmd="$cmd --overrides='{\"spec\":{\"serviceAccountName\":\"$SA\"}}'"
        cmd="$cmd -- petstore create user"
        [ -n "$RD_OPTION_RESOURCE_NAME" ] && cmd="$cmd --cr-name=$RD_OPTION_RESOURCE_NAME"
        cmd="$cmd --email=\"$RD_OPTION_EMAIL\""
        cmd="$cmd --firstName=\"$RD_OPTION_FIRSTNAME\""
        cmd="$cmd --id=\"$RD_OPTION_ID\""
        cmd="$cmd --lastName=\"$RD_OPTION_LASTNAME\""
        cmd="$cmd --password=\"$RD_OPTION_PASSWORD\""
        cmd="$cmd --phone=\"$RD_OPTION_PHONE\""
        cmd="$cmd --userStatus=\"$RD_OPTION_USERSTATUS\""
        cmd="$cmd --username=\"$RD_OPTION_USERNAME\""
        [ -n "$RD_OPTION_NAMESPACE" ] && cmd="$cmd -n $RD_OPTION_NAMESPACE"
        cmd="$cmd --dry-run=$RD_OPTION_DRY_RUN --timeout=$RD_OPTION_TIMEOUT"
        [ "$RD_OPTION_NO_WAIT" = "true" ] && cmd="$cmd --no-wait"
        eval $cmd
    keepgoing: false
    strategy: node-first
