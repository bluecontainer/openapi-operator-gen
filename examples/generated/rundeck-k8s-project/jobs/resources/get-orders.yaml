# Generated by openapi-operator-gen v0.0.10-9-ge9f872c-dirty
# List Order resources (Kubernetes pod execution)
- defaultTab: output
  description: |-
    List Order resources managed by the Kubernetes operator.
    Runs kubectl plugin inside an ephemeral Kubernetes pod.
    Uses: kubectl run ... -- petstore get orders
  executionEnabled: true
  group: resources/order
  loglevel: INFO
  name: get-orders
  nodeFilterEditable: false
  options:
  - name: namespace
    description: "Kubernetes namespace"
  - name: output
    description: "Output format"
    enforced: true
    values:
    - 'table'
    - 'wide'
    - 'json'
    - 'yaml'
    value: 'table'
  - name: k8s_token
    secure: true
    valueExposed: true
    storagePath: keys/project/petstore-operator-k8s/k8s-token
    hidden: true
  - name: k8s_url
    secure: true
    valueExposed: true
    storagePath: keys/project/petstore-operator-k8s/k8s-url
    hidden: true
  scheduleEnabled: true
  sequence:
    commands:
    - description: "List Order resources"
      scriptInterpreter: /bin/bash
      script: |-
        set -e
        IMAGE="${PLUGIN_RUNNER_IMAGE:-petstore-kubectl-plugin:latest}"
        PLUGIN_NS="${PLUGIN_NAMESPACE:-petstore-system}"
        SA="${PLUGIN_SERVICE_ACCOUNT:-petstore-plugin-runner}"
        POD="plugin-run-$(date +%s)-$RANDOM"
        cmd="kubectl --server=\"$RD_OPTION_K8S_URL\" --token=\"$RD_OPTION_K8S_TOKEN\" --insecure-skip-tls-verify"
        cmd="$cmd run $POD --image=$IMAGE --restart=Never --rm -i --quiet --image-pull-policy=Never"
        cmd="$cmd -n $PLUGIN_NS"
        cmd="$cmd --overrides='{\"spec\":{\"serviceAccountName\":\"$SA\"}}'"
        cmd="$cmd -- petstore get orders"
        [ -n "$RD_OPTION_NAMESPACE" ] && cmd="$cmd -n $RD_OPTION_NAMESPACE"
        [ -n "$RD_OPTION_OUTPUT" ] && cmd="$cmd -o $RD_OPTION_OUTPUT"
        eval $cmd
    keepgoing: false
    strategy: node-first
