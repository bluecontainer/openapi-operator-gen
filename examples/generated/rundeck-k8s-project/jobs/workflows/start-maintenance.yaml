# Generated by openapi-operator-gen v0.0.10-13-g20d778f-dirty
# Start maintenance workflow - pause all resources then confirm status
- defaultTab: output
  description: |-
    Start maintenance window: pause all resources of a kind, then confirm they are paused.
    Steps: pause (all) -> status
  executionEnabled: true
  group: workflows
  loglevel: INFO
  name: start-maintenance
  nodeFilterEditable: false
  options:
  - name: resource_kind
    description: "Resource kind to pause (e.g., Pet, Order, User)"
    required: true
  - name: reason
    description: "Reason for maintenance window"
  - name: namespace
    description: "Kubernetes namespace (defaults to project setting)"
    value: '${globals.namespace}'
  scheduleEnabled: true
  sequence:
    commands:
    - jobref:
        name: pause
        group: operations
        args: "-resource_kind ${option.resource_kind} -all true -reason ${option.reason} -namespace ${option.namespace}"
      description: "Pause all resources of specified kind"
    - jobref:
        name: status
        group: operations
        args: "-namespace ${option.namespace}"
      description: "Confirm resources are paused"
    keepgoing: false
    strategy: node-first
