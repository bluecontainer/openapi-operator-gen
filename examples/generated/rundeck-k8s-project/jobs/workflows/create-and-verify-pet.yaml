# Generated by openapi-operator-gen v0.0.10-26-g2bb48be-dirty
# Create and verify Pet resource workflow
- defaultTab: output
  description: |-
    Create a Pet resource and verify it was created successfully.
    Steps: create-pet -> describe-pet
    The create step forces JSON output so the child job can capture the
    resource name via export-var, making it available to the describe step
    as ${export.resource_name}.
  executionEnabled: true
  group: workflows
  loglevel: INFO
  name: create-and-verify-pet
  nodeFilterEditable: false
  options:
  - name: resource_name
    description: "Name for the CR (auto-generated if not specified)"
  - name: category
    description: " (JSON format)"
  - name: id
    description: ""
  - name: name
    description: ""
  - name: photoUrls
    description: " (JSON format)"
  - name: status
    description: "pet status in the store"
    enforced: true
    values:
    - 'available'
    - 'pending'
    - 'sold'
  - name: tags
    description: " (JSON format)"
  - name: namespace
    description: "Kubernetes namespace (defaults to project setting)"
    value: '${globals.namespace}'
  - name: dry_run
    description: "Preview the resource without creating it"
    enforced: true
    values:
    - 'true'
    - 'false'
    value: 'false'
  - name: no_wait
    description: "Don't wait for the resource to sync"
    enforced: true
    values:
    - 'true'
    - 'false'
    value: 'false'
  - name: timeout
    description: "Timeout for --wait (e.g., 60s, 5m)"
    value: '60s'
  - name: verify_output
    description: "Output format for the verify (describe) step"
    enforced: true
    values:
    - 'table'
    - 'json'
    - 'yaml'
    value: 'json'
  - name: target_pod_ordinal
    description: "Target specific StatefulSet pod ordinal (0-indexed)"
  - name: target_base_url
    description: "Static base URL override for the API endpoint"
  - name: target_base_urls
    description: "Comma-separated base URLs for fan-out to multiple endpoints"
  - name: target_statefulset
    description: "Target a named StatefulSet for endpoint discovery"
  - name: target_deployment
    description: "Target a named Deployment for endpoint discovery"
  - name: target_pod
    description: "Target a specific pod by name"
  - name: target_helm_release
    description: "Discover target workload via Helm release name"
  - name: target_namespace
    description: "Namespace for target workload lookup"
  - name: target_labels
    description: "Additional labels for workload discovery (key=value,key=value)"
  scheduleEnabled: true
  sequence:
    commands:
    - jobref:
        name: create-pet
        group: resources/pet
        importOptions: true
        args: "-output json"
      description: "Create Pet resource"
    - jobref:
        name: describe-pet
        group: resources/pet
        args: "-resource_name ${export.resource_name} -namespace ${option.namespace} -output ${option.verify_output}"
      description: "Verify Pet resource was created"
    keepgoing: false
    strategy: node-first
