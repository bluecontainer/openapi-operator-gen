# Generated by openapi-operator-gen v0.0.10-20-ge0145f7-dirty
# Drift remediation workflow - detect drift then show status
- defaultTab: output
  description: |-
    Periodic drift check: run drift detection with diffs, then show status overview.
    Intended for scheduled periodic execution.
    Steps: drift (show_diff) -> status
  executionEnabled: true
  group: workflows
  loglevel: INFO
  name: drift-remediation
  nodeFilterEditable: false
  options:
  - name: namespace
    description: "Kubernetes namespace (defaults to project setting)"
    value: '${globals.namespace}'
  scheduleEnabled: true
  sequence:
    commands:
    - jobref:
        name: drift
        group: operations
        args: "-namespace ${option.namespace} -show_diff true"
      description: "Run drift detection with diffs"
    - jobref:
        name: status
        group: operations
        args: "-namespace ${option.namespace}"
      description: "Show operator status overview"
    keepgoing: false
    strategy: node-first
