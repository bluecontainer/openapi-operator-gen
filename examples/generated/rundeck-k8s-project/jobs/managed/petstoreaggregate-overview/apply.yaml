# Generated by openapi-operator-gen v0.0.10-27-g49a94a0-dirty
# Apply managed PetstoreAggregate "overview" (Kubernetes pod execution)
- defaultTab: output
  description: |-
    Apply managed PetstoreAggregate "overview" to the cluster.
    Creates the resource if it doesn't exist, updates if it does.
    Runs kubectl inside an ephemeral Kubernetes pod.
    Uses: kubectl run ... -- kubectl apply -f - (with embedded CR YAML)
  executionEnabled: true
  group: managed/petstoreaggregate-overview
  loglevel: INFO
  name: apply-petstoreaggregate-overview
  nodeFilterEditable: false
  options:
  - name: namespace
    description: "Kubernetes namespace (defaults to project setting, overrides metadata.namespace)"
    value: '${globals.namespace}'
  - name: dry_run
    description: "Preview without applying"
    enforced: true
    values:
    - 'true'
    - 'false'
    value: 'false'
  - name: k8s_token
    secure: true
    valueExposed: true
    storagePath: keys/${node.clusterTokenSuffix}
    hidden: true
  - name: k8s_token_default
    secure: true
    valueExposed: true
    storagePath: keys/project/petstore-operator-k8s/k8s-token
    hidden: true
  scheduleEnabled: true
  sequence:
    commands:
    - description: "Apply PetstoreAggregate overview"
      scriptInterpreter: /bin/bash
      script: |-
        set -e
        IMAGE="${PLUGIN_RUNNER_IMAGE:-petstore-kubectl-plugin:latest}"
        PLUGIN_NS="${PLUGIN_NAMESPACE:-petstore-system}"
        SA="${PLUGIN_SERVICE_ACCOUNT:-petstore-plugin-runner}"
        POD="plugin-run-$(date +%s)-$RANDOM"
        cat <<'CRYAML' > /tmp/managed-cr.yaml
        apiVersion: petstore.example.com/v1alpha1
        kind: PetstoreAggregate
        metadata:
          name: overview
        spec:
          resourceSelectors:
            - kind: Order
            - kind: Pet
            - kind: User
          aggregationStrategy: AllHealthy
        CRYAML
        inner="kubectl apply -f -"
        _NS="${RD_OPTION_NAMESPACE:-$RD_GLOBALS_NAMESPACE}"
        [ -n "$_NS" ] && inner="$inner -n $_NS"
        [ "$RD_OPTION_DRY_RUN" = "true" ] && inner="$inner --dry-run=client"
        # Use cluster URL from node attribute if available, fall back to project global
        _CLUSTER_URL="@node.clusterUrl@"
        [ -z "$_CLUSTER_URL" ] || [ "$_CLUSTER_URL" = "@""node.clusterUrl""@" ] && _CLUSTER_URL="$RD_GLOBALS_K8S_URL"
        _K8S_TOKEN="${RD_OPTION_K8S_TOKEN:-$RD_OPTION_K8S_TOKEN_DEFAULT}"
        cmd="cat /tmp/managed-cr.yaml | kubectl --server=\"$_CLUSTER_URL\" --token=\"$_K8S_TOKEN\" --insecure-skip-tls-verify"
        cmd="$cmd run $POD --image=$IMAGE --restart=Never --rm -i --quiet --image-pull-policy=IfNotPresent"
        cmd="$cmd -n $PLUGIN_NS"
        cmd="$cmd --overrides='{\"spec\":{\"serviceAccountName\":\"$SA\"}}'"
        cmd="$cmd -- $inner"
        eval $cmd
        rm -f /tmp/managed-cr.yaml
    keepgoing: false
    strategy: node-first
